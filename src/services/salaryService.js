import cls from "../data/classifications.json";import sal from "../data/salary_steps.json";import jobs from "../data/job_titles.json";export function getSalarySteps(code,level){const c=cls.find(x=>x.code.toLowerCase()===code.toLowerCase());if(!c)return[];return sal.filter(x=>x.classificationId===c.id&&x.level===Number(level));} export function getJobTitles(code){const c=cls.find(x=>x.code.toLowerCase()===code.toLowerCase());if(!c)return[];return jobs.filter(x=>x.classificationId===c.id);}